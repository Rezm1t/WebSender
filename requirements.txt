Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
gunicorn==20.1.0
# Pin setuptools to ensure pkg_resources is present during build
setuptools==68.2.2
# Wheel can help avoid build isolation issues
wheel==0.40.0
